# Runtime dependencies for the CI data pull (.scripts/pull_data.py).
# Keep this minimal — it is installed in the "Pull Data" GitHub Actions workflow.
# Jupyter/matplotlib/notebook tooling lives in the local .venv and must NOT be
# added here; doing so previously pulled in many unused, vulnerable packages.
mysql-connector-python==9.7.0
numpy==2.3.5
pandas==2.3.3
python-dotenv==1.2.2
